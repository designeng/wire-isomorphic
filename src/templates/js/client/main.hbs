/* compilation for page: {{pageId}} */

import wire from 'essential-wire';
import pipeline from 'when/pipeline';

{{#each specs}}
import '{{name}}' from '{{path}}';
{{/each}}

console.log('hello from compiled {{pageId}} script!');